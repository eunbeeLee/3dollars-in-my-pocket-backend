spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
        dialect: org.hibernate.dialect.MySQL57Dialect
        dialect.storage_engine: innodb
        default_batch_fetch_size: 1000
        query:in_clause_parameter_padding: true
        connection.provider_disables_autocommit: true
    open-in-view: false
    hibernate:
      ddl-auto: none
  datasource:
    hikari:
      auto-commit: false
---
spring:
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
  datasource:
    hikari:
      driver-class-name: org.h2.Driver
      jdbc-url: jdbc:h2:mem:threedollar;MODE=MYSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;
      username: sa
  jpa:
    hibernate:
      ddl-auto: create
  flyway:
    enabled: false
  config:
    activate:
      on-profile: local
---
spring:
  datasource:
    hikari:
      jdbc-url: jdbc:mariadb://localhost:3306/threedollar_test
      username: will
      password: '0000'
      driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
  config:
    activate:
      on-profile: local-will
---
spring:
  jpa:
    hibernate:
      ddl-auto: none
  config:
    activate:
      on-profile: dev
---

spring:
  jpa:
    hibernate:
      ddl-auto: none
  config:
    activate:
      on-profile: beta
---

spring:
  jpa:
    hibernate:
      ddl-auto: none
  config:
    activate:
      on-profile: prod
---
